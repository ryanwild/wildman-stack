{
  "author": "",
  "dependencies": {
    "@fastify/cors": "11.2.0",
    "better-auth": "1.6.9",
    "fastify": "5.8.5",
    "pg": "8.20.0"
  },
  "description": "The authentication token issuer service",
  "devDependencies": {
    "@types/debug": "4.1.13",
    "@types/node": "25.6.2",
    "@types/pg": "8.20.0",
    "drizzle-kit": "0.31.10",
    "tsx": "4.21.0",
    "typescript": "6.0.3"
  },
  "license": "MIT",
  "main": "index.js",
  "name": "auth",
  "scripts": {
    "gen-migration": "npx --yes auth@latest generate --config=src/lib/auth.ts --dialect=postgresql --output=migrations/latest.sql --yes",
    "migrate": "npx --yes auth@latest migrate --config=src/lib/auth.ts --yes",
    "test": "echo \"Warning: no test specified\" && exit 0",
    "tsc-check": "tsc --noEmit true",
    "tsc-prod": "tsc --project tsconfig.prod.json"
  },
  "type": "module",
  "version": "0.0.1"
}
